# 🌐 MirSub - Subscription Updater

سیستم خودکار برای تست و آپدیت لیست کانفیگ‌های **VLESS** به صورت زمان‌بندی شده.

## ✨ ویژگی‌ها

- 🔄 **بروزرسانی خودکار** - هر ساعت
- 🧪 **تست واقعی** - اتصال فعلی به سرورها
- 📊 **فیلتر خودکار** - فقط کانفیگ‌های کار‌کننده
- ⚡ **موازی‌سازی** - تست سریع (10 موضوع)
- 📝 **لاگ‌گیری** - خروجی دقیق در هر اجرا

## 📋 نحوه کار

### مرحله‌ها:

1. **دریافت کانفیگ‌ها**
   ```
   curl -L -o raw.txt "https://raw.githubusercontent.com/sevcator/5ubscrpt10n/main/protocols/vl.txt"
   ```

2. **حذف تکراری‌ها**
   ```
   sort raw.txt | uniq > unique.txt
   ```

3. **تست اتصال**
   - هر کانفیگ را با `bash TCP connect test` تست می‌کند
   - فقط کانفیگ‌های موفق را ذخیره می‌کند

4. **آپدیت repository**
   ```
   git commit -m "Auto-update subscription [skip ci]"
   git push
   ```

## 🔗 استفاده

### لینک خام (Raw):
```
https://raw.githubusercontent.com/miraali1372/mirsub/main/subscription.txt
```

### در کلاینت‌های پروکسی:
**Sing-Box, V2Ray, Clash** و دیگر کلاینت‌ها میتوانند این URL را استفاده کنند.

## ⏰ زمان‌بندی

- **Cron**: `0 * * * *` (هر ساعت در دقیقه 0)
- **دستی**: از طریق `workflow_dispatch`

## 🛠️ فایل‌های مهم

```
mirsub/
├── .github/
│   └── workflows/
│       └── update-sub.yml    # تعریف Workflow
├── test_configs.py           # اسکریپت تست
├── subscription.txt          # فایل خروجی نهایی
└── README.md                 # این فایل
```

## 📈 آمار

- تعداد کانفیگ‌های دریافت شده
- تع��اد کانفیگ‌های یکتا
- تعداد کانفیگ‌های معتبر
- درصد موفقیت

## ⚠️ نکات مهم

1. **محدودیت سرعت GitHub Actions**: هر اجرا حداکثر 6 ساعت فعال است
2. **محدودیت درخواست**: ممکن است برای سرورهای بسیار بسیار زیاد نتیجه‌ای نشود
3. **تست TCP فقط**: این روش فقط اتصال TCP را تست می‌کند، نه VLESS protocol واقعی

## 🔐 حریم‌خصوصی

- هیچ داده شخصی ذخیره نمی‌شود
- فقط اتصال اولیه تست می‌شود
- هیچ trafficی از repo عبور نمی‌کند

## 📞 پشتیبانی

اگر مشکلی داشتید:
1. لاگ GitHub Actions را بررسی کنید
2. Issues یا PR ارسال کنید
3. حتی workflow را دستی اجرا کنید

## 📄 لایسنس

MIT License

---

**آخرین آپدیت**: خودکار توسط GitHub Actions